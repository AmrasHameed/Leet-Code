            word += word1[i];
        }
        if (i < word1.length) {
    for (let i = 0; i < len; i++) {
    len = Math.max(word1.length, word2.length)
    let word = ''
var mergeAlternately = function (word1, word2) {
 */
 * @return {string}
 * @param {string} word2

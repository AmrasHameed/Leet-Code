        if(!obj[rings[i + 1]]) {
    }
    let count = 0;
    for(let val in obj) {
        if(obj[val].has('R')&&obj[val].has('G')&&obj[val].has('B')) {
            obj[rings[i + 1]] = new Set()
        }
        obj[rings[i + 1]].add(rings[i])
    for(let i = 0; i < rings.length; i+=2) {
    let obj = {}
            count++
        }
    }
    return count
};

        let count = 0
        while (j < s.length && s[j] !== s[i]) {
            count++
            j++
        }
        obj1[s[i]] = count
    }
    for (let i = 0; i < distance.length; i++) {
        obj2[str[i]] = distance[i]
    let str = "abcdefghijklmnopqrstuvwxyz"
    return true
    }
        }
            return false
        if (obj1[str[i]] !== undefined && obj1[str[i]] !== obj2[str[i]]) {
};
"

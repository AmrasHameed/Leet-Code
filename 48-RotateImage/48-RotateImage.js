    for (let i = 0; i < n; i++) {
        for (let j = 0; j < n; j++) {
        }
    }
            arr[j][n - i - 1] = matrix[i][j]
    for (let i = 0; i < n; i++) {
    let arr = Array.from({ length: n }, () => Array(n).fill(0));
        for (let j = 0; j < n; j++) {
            matrix[i][j] = arr[i][j];
        }
    }
    let n = matrix.length
var rotate = function (matrix) {

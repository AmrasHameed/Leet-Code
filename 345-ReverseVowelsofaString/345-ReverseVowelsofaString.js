    for(let i = 0; i < s.length; i++) {
        if (vowels.includes(s[i])) {
            result += arr[j];
            j++;
        } else {
            result += s[i]; 
        }
    }
    return result
};
"IceCreAm"
"leetcode"
"AceCreIm"
"leotcede"
"AceCreIm"
"leotcede"
"

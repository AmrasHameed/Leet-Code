var sortSentence = function (s) {
    s = s.split(' ')
    for (let i = 0; i < s.length; i++) {
        let val = s[i][s[i].length - 1]
    }
        arr[val] = s[i].substring(0, s[i].length - 1)
    let arr = []
    return arr.join(' ').trim()
};
 */
 * @return {string}
 * @param {string} s
/**
"

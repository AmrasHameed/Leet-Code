            count++
        if (n % 2 === 0) {
    while (n !== 1) {
    let count = 0;
var integerReplacement = function (n) {
 */
            n = n / 2
        } else {
            if (((n - 1) / 2) % 2 === 0 || n === 3) {
                n = n - 1
                count++
            } else {
                n = n + 1
                count++
            }
        }
    }
 * @return {number}
 * @param {number} n

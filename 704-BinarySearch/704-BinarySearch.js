            return mid
        } 
            left = mid + 1
        } else {
            right = mid - 1
        }
    }
    return -1
        let mid = Math.floor((right + left) / 2)
    while(left <= right) {
    let right = nums.length - 1
        if(target > nums[mid]) {
        if(target === nums[mid]) {
    let left = 0
var search = function(nums, target) {
};
[

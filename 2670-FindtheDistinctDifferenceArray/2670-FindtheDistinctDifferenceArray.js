    for(let i = 0 ; i < nums.length; i++) {
        let arr1 = nums.slice(0, i +1)
    }
        let arr2 = nums.slice(i + 1, nums.length)
        arr2 = [...new Set(arr2)].length
    let arr = []
    return arr
        arr1 = [...new Set(arr1)].length
        arr.push(arr1 - arr2)
};

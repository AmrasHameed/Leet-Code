var countSeniors = function(details) {
    for(let str of details) {
        str = str.split('').slice(11).map(Number).join('')
        if(str >= 6100) {
            count++
    let count = 0
        }
    }
    return count
};

        }
            inOrder(root.left)
            inOrder(root.right)
            if(n++ < k) res = root.val
    function inOrder(root) {
        if(root) {
    return res
    }
    let res;
    let n = 0
var kthSmallest = function(root, k) {
    inOrder(root)
 */
[

    for(let num1 of aliceSizes) {
    let diff = (sum1 - sum2) / 2
    let sum2 = bobSizes.reduce((acc,x)=>acc+x,0)
    let sum1 = aliceSizes.reduce((acc,x)=>acc+x,0)
var fairCandySwap = function(aliceSizes, bobSizes) {
 */
 * @return {number[]}
 * @param {number[]} bobSizes
 * @param {number[]} aliceSizes
        for(let num2 of bobSizes) {
            if(num1 - num2 === diff) {

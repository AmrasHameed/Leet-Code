/**
 * @param {number} n
 * @return {number}
 */
var hammingWeight = function(n) {
    n = n.toString(2)
    let count = 0, i = 0
    while(i < n.length) {
        if(n[i] == 1) count++
    }
    return count
        i++
1

                }
                let index = grid[i].indexOf(num);
                if (index !== -1) {
                    grid[i].splice(index, 1);
                    max = num;
                if (num > max) {
                let num = Math.max(...grid[i]);
            if (grid[i].length > 0) {
        for (let i = 0; i < grid.length; i++) {
        let max = -Infinity;
    while (grid.length) {
    let ans = 0;
var deleteGreatestValue = function (grid) {
 */
[[1,2,4],[3,3,1]]
[[10]]
8
10
8
10
[

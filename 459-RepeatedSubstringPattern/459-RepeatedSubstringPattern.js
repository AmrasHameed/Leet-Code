            if (pattern === s) {
            let sub = s.substring(0, factor);
            let pattern = sub.repeat(len / factor);
        if (len % factor === 0) {
    for (let factor = 1; factor <= len / 2; factor++) {
                return true;
            }
        }
    }
    return false;
    let len = s.length;
var repeatedSubstringPattern = function(s) {
 */
 * @return {boolean}
 * @param {string} s
};
"

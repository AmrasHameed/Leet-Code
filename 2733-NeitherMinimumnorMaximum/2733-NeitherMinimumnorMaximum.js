    let min = Math.min(...nums);
    let max = Math.max(...nums);
        if (num !== min && num !== max) {
            return num;
        }
    for (let num of nums) {
 */
var findNonMinOrMax = function (nums) {
    }
    return -1;

};

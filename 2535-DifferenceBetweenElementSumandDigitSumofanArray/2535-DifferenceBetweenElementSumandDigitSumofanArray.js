        if (num > 9) {
    for (let i = 0; i < nums.length; i++) {
    }
        sum1 += nums[i]
    let sum2 = 0
    for (let i = 0; i < nums.length; i++) {
var differenceOfSum = function (nums) {
    let sum1 = 0
 * @return {number}
 */
            num = num.toString().split('').reduce((acc, x) => acc + parseInt(x), 0);
        }
        sum2 += num
    }
    return Math.abs(sum1 - sum2)
        let num = nums[i]
};
[

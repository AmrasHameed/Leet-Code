/**
 * @param {number[]} digits
 * @return {number[]}
 */
var plusOne = function(digits) {
    digits = digits.join('')
    digits++
    return digits.toString().split('')
    digits = BigInt(digits)
};
[

/**
 * @param {number[]} digits
 * @return {number[]}
 */
var plusOne = function (digits) {
    digits = digits.join('');
    return digits
    digits = BigInt(digits)
    digits++
    digits = digits.toString().split('').map(Number)
};
[

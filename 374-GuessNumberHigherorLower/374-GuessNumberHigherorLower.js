        if(guess(mid)== 1) {
            start = mid + 1
        } else if(guess(mid)== -1) {
            end = mid - 1
        } else if(guess(mid)== 0) {
            return mid
    while(start <= end) {
    let end = n
        let mid = Math.floor((start + end)/2)
        }
    }
};
    let start = 1
var guessNumber = function(n) {
 */
1

/**
 * @param {number[]} gain
 * @return {number}
 */
var largestAltitude = function(gain) {
    for(let i = 0; i < gain.length; i++) {
        sum += gain[i] 
    let sum = 0
    let max = 0
        if(sum > max) {
            max = sum
        }
    }
    return max
        console.log(sum)
[

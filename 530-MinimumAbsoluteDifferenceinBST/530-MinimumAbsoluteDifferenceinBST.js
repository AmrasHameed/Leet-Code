        }
    }
    inOrder(root)
    for (let i = 0; i < arr.length - 1; i++) {
        val = arr[i + 1] - arr[i]
    let min = arr[1] - arr[0]
        if (val < min) {
            min = val
        }
            inOrder(root.right)
            arr.push(root.val)
            inOrder(root.left)
        if (root) {
    function inOrder(root) {
    let arr = []
var getMinimumDifference = function (root) {
 */
[

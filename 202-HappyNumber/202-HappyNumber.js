        }
        seen.add(result);
            return false;
        if (seen.has(result)) {
        result = arr.map(Number).reduce((acc, x) => acc + (x * x), 0);
        let arr = result.toString().split('');
    while (result !== 1) {

    }

    return true;
};

19
2
true
false
true
false
1

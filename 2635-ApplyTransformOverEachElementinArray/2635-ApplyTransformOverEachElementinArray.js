 * @return {number[]}
 */
var map = function (arr, fn) {
    while (i < arr.length) {
    }
    return arr
        arr[i] = fn(arr[i], i)
 * @param {Function} fn
 * @param {number[]} arr
    let i = 0;
        i++
};
f

    }
            arrev.push(arr[i])
        }else {
            arrod.push(arr[i])
        }
    console.log(arrev,arrod)
    }
        }
            return arrod[i]
        if(!arrev.includes(arrod[i])) {
    for(let i = 0; i < arrod.length; i++) {
};

        }else {
            arrev.push(arr[i])
        if(i%2===0) {
    for(let i = 0; i < arr.length; i++) {
    let arrod = [], arrev = [];
            arrod.push(arr[i])
        }
    }
    let arr = paths.flat(2)
var destCity = function (paths) {
 */
 * @return {string}
 * @param {string[][]} paths
/**
    console.log(arrev,arrod)
    for(let i = 0; i < arrod.length; i++) {

    for(let i = 0; i < words.length; i++) {
    }
    let count = 0
        if(s.startsWith(words[i])) {
var countPrefixes = function(words, s) {
 */
 * @return {number}
 * @param {string} s
            count++
        }
    return count
};
[

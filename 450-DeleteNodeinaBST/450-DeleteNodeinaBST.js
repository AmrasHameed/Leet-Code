}
    return min(root.left)
    }
        return root.val
    if(!root.left) {
var min = function(root) {
 */
 * @return {TreeNode}
 * @param {number} key
 * @param {TreeNode} root
/**
 */
 * }

var deleteNode = function(root, key) {
    if(root == null) {
        return root
[
